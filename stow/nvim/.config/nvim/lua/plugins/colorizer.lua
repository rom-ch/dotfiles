require("colorizer").setup({
	user_default_options = {
		mode = "background", -- or "foreground" / "virtualtext"
		names = false, -- disable named colors ("red", "blue")
		tailwind = true, -- highlight tailwind classes
	},
})
